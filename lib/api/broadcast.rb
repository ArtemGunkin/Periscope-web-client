module Api
  class Broadcast < OpenStruct
    # def method_missing(method, *arguments, &block)
    #   if respond_to?(method.to_s.chomp('?'))
    #     send(api_method)
    #   else
    #     super
    #   end
    # end
  end
end
